// Deliberately thin for the First Milestone: the actual translation prompt
// and session config are built once, in Rust (src-tauri/src/session.rs /
// websocket.rs), to avoid duplicating drift-prone logic in two languages.
// This file exists only as the designated home for future UI-facing
// translation helpers (e.g. formatting), per the spec's folder layout.

export {};
