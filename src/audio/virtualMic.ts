// STUB for the First Milestone. Virtual microphone output (so translated
// speech can be routed into Google Meet as a fake mic input) is Phase 2 work
// — see src-tauri/src/virtual_mic.rs for the matching Rust-side stub.
// TODO(phase2): wire this into a "Route to Google Meet" UI toggle once
// src-tauri/src/virtual_mic.rs has a real VirtualMicOutput implementation.

export function isVirtualMicSupported(): boolean {
  return false;
}
